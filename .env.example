# ── Printer web UI login ──────────────────────────────────────────────────
VITE_APP_USERNAME=changeme
VITE_APP_PASSWORD=changeme

# ── Fake SMTP server credentials ──────────────────────────────────────────
# Set these to whatever your provisioning script will send.
# The smtp-server microservice accepts ONLY this user/password pair.
SMTP_VALID_USER=printer@local.test
SMTP_VALID_PASSWORD=changeme
